pipeline {  
    agent {label 'nova'}
   
    stages {
        stage ("deploying"){
            steps{                     
                sh 'cat /etc/passwd'
                
            }
        }
    }
}
